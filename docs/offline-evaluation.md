# Superseded mocked offline evaluation

The former 20-case offline report for corpus `2026-09-12.1` and prompt `1.0` is superseded.

Current offline results are generated by `npm run eval:offline` under the ignored `evaluation/results/` directory. The runner assembles mock answers from each case's expected facts and allowed evidence IDs. These results verify pipeline and reporting behaviour only; they are not model-accuracy results. Live model responses and human factual review remain pending.
